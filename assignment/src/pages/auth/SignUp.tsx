import { SIGNUP_INPUTS } from "../../constant/form-static-data";
import CustomForm from "./CustomForm";

const SignUp = () => {
  return <CustomForm input_values={SIGNUP_INPUTS} />;
};

export default SignUp;
